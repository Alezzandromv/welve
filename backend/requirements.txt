# Versiones fijadas (==) a las últimas estables verificadas contra la suite de tests y el
# smoke test manual (ver docs/DEPENDENCIAS.md para el detalle de la verificación). Se fijan en
# vez de dejar pisos abiertos (`>=`) para que `pip install -r requirements.txt` sea
# reproducible entre entornos — antes de este cambio, un install limpio podía traer versiones
# distintas a las probadas aquí.
fastapi==0.141.1
bcrypt==5.0.0
uvicorn[standard]==0.52.4
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
pydantic==2.13.5
pydantic[email]==2.13.5
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
python-multipart==0.0.32
celery==5.6.3
redis==8.1.0
httpx==0.28.1
python-dotenv==1.2.3
ruff==0.16.5
pytest==9.1.1
pytest-asyncio==1.4.0
